

This repo contains the PCB design and microcontroller code related to the GP22 TDC device. 
It is the result of merging two seperate repos. 

You should copy the two files "post-checkout" and "post-commit" into .git/hooks/ if you want the automatic versioning to work. 